Instructions for running the project:
1.	First, you need to merge the CSV files using the provided script (f1_prepare_data_for_ai.py). This will prepare and clean the data for the prediction model.
2.	Once the data is ready, you can run the prediction model (f1_prediction_model.py) to get the driver positions based on the features in the dataset.
